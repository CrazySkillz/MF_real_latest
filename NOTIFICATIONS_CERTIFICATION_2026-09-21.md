# Global Notifications and Bell Certification

Validation completed: 2026-09-22. Scope: the GA4-first global Notifications page, top-bar bell, active KPI/Benchmark alerts, alert-email path, and directly related retained APIs. This is a bounded production-readiness certification for the exact deployed application revision below, not a certification of other app sections or unconfigured ad/social platforms.

**Result: CLEAN-CERTIFIED for the stated GA4-first scope at `e6a9d7cb17e9389bfa12c80f9f9563da44f33bb1`.** The evidence distinctions and exclusions below are part of this result. Any later implementation, contract, configuration, or deployment change affecting these paths requires impact review before carrying the result forward.

## Exact runtime and configuration

- Local `main`, refreshed `origin/main`, and Render `/api/health` all resolved to `e6a9d7cb17e9389bfa12c80f9f9563da44f33bb1`; Render returned `nodeEnv: production` and HTTP 200 at 2026-09-22 13:20:52 UTC. `/api/health` does not expose the production Node.js version.
- Production target: `https://marketforensics.onrender.com`; GA4-first application. Meta, Google Ads, Instagram, and TikTok are unconfigured and excluded from source-specific certification.
- Owner inventory after cleanup: 9 clients, 38 campaigns across 8 clients with campaigns. Live tests used existing `Campaign2` (`eee3e654-b736-4e8e-86ec-1050e4d905c0`), two disposable campaigns in one disposable client, and a separate synthetic foreign-owner fixture. No existing client/campaign was deleted.
- Alert-email test: one temporary GA4 Benchmark, one recipient (`tester_for_mm@outlook.com`), `daily` frequency, explicit opt-in only after its API-created alert existed. The opt-in was applied to that exact disposable row without calling the immediate-send route; after opt-in, no further mutating API targeting the opted-in Benchmark was called before the email audit appeared. Source default reminder interval is 900,000 ms; the deployed interval environment value is not exposed. The natural background-only audit appeared 14 minutes 27 seconds after opt-in, consistent with the default cadence. Effective scheduler operation is proven by this run, not by assuming the deployed environment value.
- The scheduled audit was created at 2026-09-22 13:15:16 UTC, had one attempt and a dedupe key, used `mailgun-api`, and reached provider-confirmed `delivered` at 13:15:22 UTC. Provider acceptance alone is not described as delivery. This new message's actual inbox receipt was not independently checked; the user previously confirmed inbox receipt of the separate immediate test notification.

Required references were read in the repository-prescribed order: `AGENTS.md`, `ARCHITECTURE_USER_JOURNEY.md`, `PRODUCTION_READINESS.md`, `GA4/README.md`, `GA4_DEVELOPMENT_WORKFLOW.md`, `TARGETED_DESTRUCTIVE_VISIBILITY_AUDIT.md`, and `GA4/KPI_BENCHMARK_ALERTS_NOTIFICATIONS_PRODUCTION_READINESS.md`. Earlier evidence was reused only for the specific implementation and contracts confirmed unchanged; changed KPI deletion was instead checked against its later GA4 KPI lifecycle evidence and the current deployed tests.

## Separate results

1. **Bell and active-alert state — passed.** On the exact deployed revision, a real active Benchmark alert lit the bell, the bell navigated to Notifications, and its disabled/current-page state settled correctly. Owner-scoped live API data and anonymous 401 behavior were checked. Empty and error UI states were exercised with intercepted fixtures, not by damaging production data. The client/campaign deletion query-refetch contracts were confirmed unchanged from their earlier validation; no live UI delete was repeated. No unread/read, Mark All as Read, or dismiss control appeared.
2. **Active-alert list, filters, pagination, navigation — passed.** Deployed-browser fixture coverage exercised priority, client, campaign, and date filters; pagination; selected/missing states; and a Benchmark deep link. KPI deep-link and unit-formatting branches were code-traced and regression-guarded, not all live-clicked or live-rendered. A separate read-only deployed-browser run rendered a real count-valued alert with its value/threshold and followed its exact Benchmark highlight link. The real alert check blocked eight reconciliation POSTs and observed zero other mutation attempts; fixture tests supplied cases not naturally present in the small live list.
3. **Reconciliation and lifecycle — passed.** Earlier exact-revision Campaign2 validation exercised Benchmark create/edit, breach resolution, re-breach, alert identity/link, and delete. This run exercised an active-alert GA4 KPI create/delete and active-alert campaign/client cascades, verified neighboring campaign visibility was unchanged, and proved history was soft-hidden. A disposable synthetic foreign-owner client/campaign/notification was excluded from owner lists and direct reads; retained per-alert legacy PATCH/DELETE returned 404 without changing it. Local failure-injection/visibility tests passed 53/53; the two formerly skipped isolated real-PostgreSQL KPI/Benchmark concurrency tests passed 2/2. Stale/orphaned/non-breaching suppression and retry/failure safety are covered by the focused code/tests and live owner visibility inventory; production fault injection was not performed.
4. **Email scheduling and delivery — passed to provider-confirmed delivery.** The API opt-in/recipient flow and immediate email were previously exercised, with user-confirmed inbox receipt. This run separately observed a natural timer-created daily-frequency audit for exactly one opted-in test recipient; it was atomically deduplicated to one audit/attempt and reached Mailgun `delivered`. The temporary row was immediately opted out after the claim and deleted after delivery. Retry and alternate audit states were validated locally, not by inducing a production provider failure.
5. **Combined Notifications surface — passed for the bounded GA4-first scope.** The bell, real owner-scoped list, filters/links, GA4 alert lifecycle, destructive visibility, cross-owner fail-closed behavior, natural scheduled email, and provider-delivery evidence agree on the same deployed revision. Retained inbox-era APIs remain access guarded but are not visible product features.

## Verification and cleanup

- Focused notification/alert matrix on this application revision: 213 passed; two isolated PostgreSQL tests were initially skipped only because a test database was absent. They later passed 2/2 against a disposable loopback-only PostgreSQL instance, which was stopped and removed. The focused deletion/visibility packet passed 53/53. These packets overlap and are not summed as distinct tests.
- TypeScript validation and the production build passed once on the exact application implementation; subsequent validation added only untracked scripts and this certificate, not application code. No unrelated full suite was run.
- Final read-only production inventory: **0** disposable clients, campaigns, KPIs, or Benchmarks remain; **5** related notification-history rows remain soft-hidden (**0** active: 2 campaign, 1 KPI, 2 Benchmark deletions); **1** scheduled test email audit remains, provider-confirmed delivered. The separate synthetic foreign-owner fixture was deleted exactly. Validation sign-in sessions/tokens were revoked.
- Audit harnesses: `scripts/notifications-campaign2-authorized-validation.ts`, `scripts/notifications-deployed-ui-readonly-validation.ts`, `scripts/notifications-final-authorized-validation.ts`, `scripts/notifications-final-live-ui-readonly.ts`, and `scripts/notifications-final-cross-owner-validation.ts`. These are local evidence scripts, not deployed application changes.

## Exclusions and remaining gates

- No in-scope blocking gate remains for this exact GA4-first, provider-confirmed certification. Inbox receipt of the **new scheduled** message is not claimed; if inbox-level proof for that specific send is required, it is a separate external check.
- Production failure injection, forced retry, artificially induced empty/error states, and a naturally populated multi-page list were not performed. Those paths have focused regression and deployed fixture evidence only; they are not described as live production incidents.
- No Meta, Google Ads, Instagram, or TikTok source-specific alert behavior is certified. No legacy inbox controls are restored. The deployed reminder interval environment variable and production Node.js version are not exposed; observed runtime behavior is recorded instead.
- At the time of validation, no production application code was edited, no protected prior certificate or readiness section was changed, no ledger was updated, and no commit or push had been made. The pre-existing uncommitted `APP_PRODUCTION_READINESS.md` change was preserved. Later documentation/script-only commits are not the validated application runtime.
