# GA4 Insights → What to investigate next subsection certificate — 2026-09-18

## Decision and exact boundary

**CLEAN-CERTIFIED for the on-screen “What to investigate next” findings and Total/High/Medium cards on exact deployed revision `6673a976f98d853b9eb37ddc99a2afc195f302d9`.** This certificate covers the findings engine and these cards only. The authenticated, read-only deployed check used owned campaign `eee3e654-b736-4e8e-86ec-1050e4d905c0`, selected active OAuth property `542352127`, saved filters `yesop_retargeti` and `yesop_email_nurture`, and `Europe/Amsterdam` reporting time. The prior empty-state wording fix `59f06f2057f8d62abc46bc3a47c361fd37fc31b3` and the findings logic fix are included in the deployed revision.

The findings are calculated from **persisted rows imported for the selected campaign and property**. The connection's initial historical-import boundary is `2026-08-09`; the campaign was created `2026-09-08`. Stored rows can change during later scheduled or explicit refreshes. There is no immutable creation-day snapshot in this path. A September 17 row had lagged GA4, causing a temporary three-day drop finding on the older deployed revision. The earlier scoped refresh corrected that row. The exact new deployed revision used provider-verified saved history through September 17, including September 4–7 before campaign creation, and showed one positive conversion finding.

The user authorized the wording and findings-logic commits and push. Only findings-related client code and findings-specific test assertions were changed; the certified Trends, Data Summary, coverage route, KPI/Benchmark, financial producers, contracts, and certificates remain untouched. The earlier manual refresh used the existing production lifecycle, changed this campaign's stored GA4 daily facts, and ran its dependent recompute path; it did not change any section's code or contract. The master ledger was not updated. No whole-Insights or Reports claim is made.

## Certified findings-only logic

- **Root cause:** findings ignored provider-confirmed zero-activity days and could emit a false trend finding when saved rows disagreed with GA4 despite a nominal freshness flag. An intermediate local candidate incorrectly applied the Trends display's campaign-creation filter; the user's correction requires all imported campaign history, including pre-creation dates, to remain eligible.
- **Implemented path:** `findingsDailyRows` uses the campaign/property's existing 60-day daily response. When the unchanged owner/property-scoped coverage route verifies the same property, reporting cutoff, and timezone, it uses that route's provider-checked saved rows plus only verified zero dates inside the 60-day response. A separate `findingsRollups` consumes those rows without a creation-date filter; Data Summary and Trends calculations remain unchanged. Findings wait for the first coverage result. A confirmed same-property/date/timezone saved/provider mismatch suppresses daily trend comparisons and the standalone top-channel finding. Channel details in current KPI/Benchmark recommendations can still appear when the breakdown reconciles and findings/Data Summary Sessions and Conversions match, including on a daily mismatch or stale response.
- **Selected-campaign evidence:** the read-only deployed check found stored activity on September 4–7 before the September 8 campaign creation date. Provider coverage verified the absent September 8 and 11 dates as zero. The September 11–17 seven-day window totaled 242 Sessions, 43 Conversions, and $8,049.60 GA4-native Revenue; September 4–10 totaled 221 Sessions, 37 Conversions, and $7,469.10. Conversions rose 16.2%, crossing the Low positive-conversion threshold of 15% with sufficient prior volume. The deployed UI showed that exact positive finding; the 7-day branch took precedence over the shorter 3-day comparison.
- **Validation:** 13 focused findings and adjacent read-only dependency test files passed (69 tests); `npm run check`, `npm run build`, and `git diff --check` passed on the pushed source. `scripts/ga4-insights-findings-live-readonly.ts` checked the exact deployed SHA, provider-verified historical dates, read-only database/API/UI parity, card content/counts, anonymous access, and browser-only stale/mismatch/unavailable withholding. Earlier checks on the discarded creation-date candidate are historical evidence only.
- **Upstream historical scope:** financial and saved KPI/Benchmark performance findings continue to use their read-only campaign-scoped to-date inputs, which may include pre-creation history. This matches the clarified requirement. The imported Revenue and Spend to-date APIs have their own source-window semantics; no creation-date allocation or upstream behavior change is claimed.

## Source, API, and UI path

| Stage | Evidence and boundary |
|---|---|
| Producer/storage | The existing GA4 import/refresh pipeline materializes `ga4_daily_metrics` by campaign, property, and calendar date. `server/storage.ts:getGA4DailyMetrics` filters all three keys. A provider refresh atomically replaces only the requested campaign/property/date window. Connection configuration holds the saved filter and import-start date; the daily rows are current persisted facts, not immutable setup values. |
| Daily API | The page requests `GET /api/campaigns/:id/ga4-daily?days=60&propertyId=542352127&readOnly=1`. The existing route checks campaign access, selects that campaign's connection, reads stored rows, and returns the Amsterdam completed-day cutoff and freshness state. The observed response reported `validationReadOnly:true`, `providerRefreshAttempted:false`, `refreshIsStale:false`, and cutoff `2026-09-17`. Unauthenticated access returned 401. The route/storage files remained unchanged from the prior selected-property ownership checks, including non-owner 404. |
| Daily transformation | `normalizeGA4InsightsDailyRows` rejects bad dates and invalid numbers, excludes dates after the cutoff, and deduplicates by date. `buildGA4InsightsRollups` uses exact adjacent 7+7 or 3+3 calendar-day windows. A missing day prevents that comparison; a valid explicit zero row counts as observed. Findings use saved campaign/property rows from the 60-day response, or the coverage route's provider-checked saved rows and verified zero dates when that scoped check succeeds. If coverage is unavailable or unverified without a confirmed mismatch, complete observed windows can still produce trend findings; absent dates are not inferred as zero. Findings do not use the Trends creation-date display filter. |
| Other inputs | Financial rules use the existing campaign GA4 to-date, imported Revenue/Spend and currency-guarded financial inputs. KPI and Benchmark rules use campaign-scoped `google_analytics` lists, verified current-value consumer states, and saved target period/configuration; property-compatible snapshot history supplies historical annotations such as streaks, trends, and average variance. Channel details require a selected-property breakdown whose dates, Sessions, and Conversions reconcile with the imported daily window and findings rollup; a failed or mismatched breakdown suppresses those details. These upstream implementations are read-only dependencies. |
| Output | `client/src/pages/ga4-metrics.tsx` creates one `insights` array, sorts High → Medium → Low, shows its first 12 items in fixed group order (setup, targets, trends, finance, context), and discloses `+ N more findings` if capped. The tracker cards count the entire same array and separately count High and Medium; positive, informational, and Low items are included in Total. Cards expose ID, category, severity, title, description, recommendation, basis, and confidence. |

## Exact-revision displayed-finding inventory

| Displayed finding | Trigger and exact evidence | Severity, basis, confidence, recommendation |
|---|---|---|
| `positive:conversions:wow` — **Conversions up 16.2% week-over-week** | Provider-verified campaign/property rows and verified zero days completed both adjacent seven-day windows. September 11–17 had 43 Conversions from 242 Sessions; September 4–10 had 37 Conversions from 221 Sessions. The 16.2% increase exceeds the +15% positive threshold, and prior Conversions exceed the required 5. The earlier window includes September 4–7 before campaign creation. The 7-day branch suppresses the 3-day fallback. | Low severity (displayed as **Positive**), basis **GA4 completed daily history for this campaign/property, including pre-creation dates and verified zero days when available**, confidence **Medium**. Recommended check: **Check which traffic sources and conversion paths contributed before changing campaign delivery.** |

The exact deployed UI showed **Total 1 / High 0 / Medium 0**. Its full findings array contained this one item; the visible card's ID, title, description, severity, basis, confidence, and recommendation matched it. There were **0 hidden findings**. The same read-only check independently summed selected-property database rows, compared them with provider-verified saved rows, and confirmed that unauthenticated campaign daily reads returned 401.

## Historical older-revision check

| Displayed finding | Trigger and exact evidence | Severity, basis, confidence, recommendation |
|---|---|---|
| **None** | Both adjacent 3-day windows are complete; 7-day windows are incomplete. Current `2026-09-15`–`17`: 100 Sessions, 19 Conversions, $3,392.70 GA4-native Revenue, 19.0% conversion rate. Prior `2026-09-12`–`14`: 142 Sessions, 24 Conversions, $4,656.90, 16.9%. Sessions fell 29.6%, Revenue 27.1%, and Conversions 20.8%: none reaches the 3-day volume-drop thresholds of 30%, 35%, and 30%. Conversion rate increased, and no other configured rule fired. | No severity, basis, confidence, or recommendation was displayed then. The older deployed empty-state sentence overclaimed “No issues detected” and prompted KPI/Benchmark setup regardless of configuration; the later deployed wording fix narrowed it to the actual rule outcome. |

The deployed UI showed **Total 0 / High 0 / Medium 0**, no visible finding, and no hidden-count notice. The full generated findings array was empty, so tracker counts and display agree. The selected campaign has **0 GA4 KPIs and 0 GA4 Benchmarks**, so no target finding is expected. Before refresh, the September 17 stored row contained 8 Sessions, 1 Conversion, and $162.18, and the page correctly exposed `anomaly:volume:3d` with Low confidence, Medium severity, imported-data basis, and an import-completeness recommendation. The corrected September 17 row contains 35 Sessions, 7 Conversions, and $1,135.20; that earlier finding was withheld after recomputation. A browser-only stale-response fixture had also withheld the prior anomaly and shown the stale-history finding instead; it did not change production data.

## Rule inventory, priority, and withholding

These are the possible finding families in this on-screen engine. The exact new deployed check emitted `positive:conversions:wow`; all other families were withheld or had false triggers for this selected campaign. Each emitted item has a description and recommended check; its basis and confidence are attached centrally before rendering.

| Family and possible IDs | Trigger/severity and recommended investigation | Deduplication/withholding |
|---|---|---|
| Daily and target integrity: `integrity:daily_history_unavailable`, `integrity:daily_history_stale`, `integrity:daily_history_outdated`, `integrity:target_lists_unverified`, `integrity:analytics_history_primary_property_only`, `integrity:target_period_mismatch`, `integrity:kpi_blocked:*`, `integrity:bench_blocked:*`, `integrity:targets_unverified`, `integrity:kpi_invalid_config:*`, `integrity:bench_invalid_config:*`, `integrity:analytics_history_unavailable`, `info:scheduler_no_history` | Failed initial daily read is High; stale last-good daily data is Medium; confirmed saved/provider mismatch is High and recommends refreshing this campaign's GA4 data before reviewing trends. Failed target lists without verified rows are High. Wrong-property history and period mismatch are Medium. Blocked/invalid saved targets and failed history are High; unverified target evaluations are High for unavailable inputs, otherwise Medium; absent snapshots are informational Low. Checks direct the user to restore/verify the named source, target, period, or snapshot. The mismatch basis is provider daily values plus saved campaign/property rows, with High confidence. | A failed list is one consolidated item per affected list set. Shared unverified target evaluations are one item with affected names/reasons. Blocked and invalid target rows are individually named. KPI/Benchmark performance is withheld for ineligible inputs, invalid configuration, or incompatible period; missing/wrong-property snapshot history withholds historical annotations only. Failed, stale, or confirmed-mismatched daily history withholds daily trend comparisons and standalone top-channel context, while reconciled channel details may remain in KPI/Benchmark recommendations. |
| Financial input/zero checks: `financial:ga4_to_date_unavailable`, `financial:ga4_to_date_stale`, `financial:revenue_missing`, `financial:spend_missing`, `financial:spend_no_revenue`, `financial:revenue_no_spend`, `financial:ga4_no_completed_window`, `financial:negative_revenue`, `financial:negative_spend`, `financial:negative_roi`, `financial:roas_below_1` | Missing GA4 lifetime totals, missing configured Revenue/Spend, spend with verified $0 Revenue, negative Revenue/Spend, and ROI ≤ -20% are High. Stale lifetime totals, verified $0 Spend with Revenue, no completed GA4 day, ROI between -20% and 0, and ROAS below 1x are Medium. Recommendations check named sources/events, windows, attribution, or spend/revenue signs. Basis is to-date source configuration/totals; confidence is High. | Missing configuration requires verified input state; a valid $0 is a separate finding. ROI/ROAS require positive denominators and verified financial inputs; no completed day and negative Spend withhold invalid dependent values. Generic provenance summaries are not counted as findings. ROI and ROAS may both be disclosed because they are separately labeled financial checks, not hidden duplicates. |
| Saved targets: `kpi:*`, `bench:*`, `positive:kpi:*` | A verified comparable KPI below target uses its saved priority (High/Medium/Low) and names current versus effective target, progress, and a metric-specific investigation. A verified Benchmark behind target is High or Medium from its status and names current versus benchmark. A verified KPI at least 10% above target yields a Low positive item to review target calibration. Basis is saved target plus current values; confidence Medium. | Lists are campaign/platform scoped. Invalid, blocked, unverified, or period-incompatible targets do not produce performance claims. Snapshot streak/trend context is included only for the selected primary property with usable history. Current campaign has no targets, so these branches were locally tested/read-only traced but not exercised by this live campaign. |
| Complete 7-day comparisons: `anomaly:cr:wow`, `anomaly:pvps:wow`, `anomaly:sessions:wow`, `anomaly:revenue:wow`, `anomaly:conversions:wow`, `positive:sessions:wow`, `positive:revenue:wow`, `positive:conversions:wow` | Two complete adjacent 7-day windows are required. Conversion-rate, Sessions, Revenue, and Conversions drops beyond 15/20/25/20% are High; pageviews per session down 20% is Medium. Positive Sessions/Revenue/Conversions use +15/+20/+15% with prior-volume guards and are Low. Checks identify landing pages, events, channel volume, and revenue quality. Basis is imported completed daily history; confidence Medium. | The 7-day branch takes precedence over the 3-day fallback. A zero prior denominator cannot create a percentage-drop or positive-volume finding. Channel detail is added only after breakdown reconciliation. |
| Short 3-day comparisons: `anomaly:cr:3d`, `anomaly:pvps:3d`, `anomaly:volume:3d`, `anomaly:sessions:3d`, `anomaly:revenue:3d`, `anomaly:conversions:3d`, `positive:sessions:3d` | Used only when 7-day comparison is unavailable and both adjacent 3-day windows are complete. Conversion-rate down 25%, Sessions/Conversions down 30%, Revenue down 35%, and the combined volume item are Medium; engagement depth down 30% and Sessions up 25% are Low. Checks focus on import completeness, source volume, landing pages, event setup, and confirmation over a longer period. Basis is imported completed daily history; confidence Low. | When Sessions, Revenue, and Conversions all cross their drop thresholds and conversion rate has not fallen, one `anomaly:volume:3d` replaces the three individual volume-drop items. Conversion-rate and engagement-depth findings remain separate signals. Stale or missing daily data suppresses all comparisons. |
| History and channel context: `anomaly:not-enough-history`, `info:top_channel` | If neither comparison window is complete, a Low history item names each missing-window count and asks to verify zero-activity versus incomplete import; its basis is imported daily history and confidence High. If a complete last 7 days and a reconciled multi-channel breakdown show one channel above 70% of Sessions, a Low channel item recommends checking that channel's movement; its basis is GA4 campaign breakdown and confidence Medium. | A valid zero row can complete a window; absent dates cannot. Top channel is withheld for stale daily data, mismatched/failed breakdown, fewer than two channels, or share ≤70%. Generic channel context does not count as a finding. |

The engine's stable severity sort is applied before the 12-item cap. Grouping changes display placement within the visible 12, without changing the underlying tracker counts. Positive and informational badges are display labels for Low-severity items. Browser PDF generation also consumes the `insights` array, so the revised findings can appear there; that downstream renderer and scheduled/server PDFs are **unverified and excluded**, not silently claimed unchanged or certified.

## Gates and evidence

| Gate | Result |
|---|---|
| Exact deployed revision | PASS: local `main`, pushed `origin/main`, production `/api/health`, and the authenticated browser check all identified full commit `6673a976f98d853b9eb37ddc99a2afc195f302d9`. The initial post-push health check still showed old SHA `7eab51552b371dc0002855c9e46aa9d7bb90e22e`; no new-revision claim was made until the deployment changed. |
| Selected source and stored inputs | PASS: transaction-read-only DB inventory joined the campaign to its owned client and active primary OAuth property, confirmed the two saved filters, Amsterdam time, `2026-08-09` import boundary, and 0 GA4 KPI/Benchmark rows. The exact-revision check independently compared 14 calendar dates, including four pre-creation rows and two provider-verified zero days, with selected-property stored records. The daily GET was `readOnly=1`, `validationReadOnly:true`, `providerRefreshAttempted:false`, and `refreshIsStale:false`. Anonymous daily GET returned 401. |
| Scoped refresh and source accuracy | PASS for the existing unchanged refresh dependency: one earlier user-directed call to the owner-guarded `POST /api/campaigns/:id/ga4-daily-scheduler/run-now` returned 200 and `lastRunStatus:success` for this campaign; it suppressed the global alert sweep and ran the existing daily refresh plus KPI/Benchmark recompute. The September 17 stored row was corrected. On the exact new deployed revision, the provider coverage GET returned `verified:true`, and the read-only validator reconciled its completed-day rows and zero dates with stored campaign/property rows. No production mutation was used for the exact-revision check. |
| Source/API/UI arithmetic and tracker parity | PASS on the exact revision: 7-day windows were 242 Sessions / 43 Conversions / $8,049.60 and 221 / 37 / $7,469.10. The independent 16.2% conversion increase matched the single `positive:conversions:wow` finding. The visible card's ID, title, description, severity, basis, confidence, and recommendation matched the generated item. Tracker values were 1/0/0; one item was visible and none hidden. The validator made GET-only app requests and used a read-only database transaction; temporary Clerk sign-in/revoke handled authentication. |
| Missing, valid zero, stale, and deduplication | PASS: live provider coverage confirmed zero-activity days without inventing rows. Focused tests cover explicit zero versus missing day, initial daily failure, stale suppression, target-list failure, true financial zero versus absent source, 3-day combined-volume deduplication, and hidden-count disclosure. Exact-revision browser-only stale, confirmed provider mismatch, and initial daily-unavailable fixtures each withheld the positive trend advice and kept tracker counts aligned. These fixtures did not change production data and are not claims of natural provider failure. |
| Local engineering gates | PASS on the pushed source: 13 focused findings and adjacent read-only dependency files/69 tests; `npm run check`; `npm run build`; `git diff --check`. TypeScript was rerun after adding the local read-only validator. The prior 22-file/115-test check on old deployed source is historical only. No certified test or contract was edited. |

The broader `server/ga4-insights-report-parity-regression.test.ts` contains a pre-existing assertion for the literal “Exact completed-day window” in another Insights/report copy path; that phrase was absent before these finding changes. It fails in the broad suite and is **not a passing test**. That protected report-copy test and report/PDF certification are outside this on-screen subsection gate. The smallest separate fix is to reconcile that assertion with the already certified copy, with approval before changing protected tests or code. No such fix was made or counted toward this certificate.

## Exclusions and remaining gates

**Remaining gates inside this certified on-screen boundary:** none for the selected owned campaign/property and the tested negative branches on this exact revision. The final product diff changed only the on-screen findings path and findings-specific assertions. A natural timer firing on this commit and a live campaign with configured KPI/Benchmark target findings were not observed; those are explicit exclusions below, not inferred passes. No credential reconnect, certified-route change, or data cleanup was required.

Outside this certificate: an immutable campaign-creation snapshot, naturally fired scheduler success on this commit, live target findings for a campaign with configured KPIs/Benchmarks, other campaigns/properties, the other Insights subsections, existing certified sections, browser/server PDFs, Reports, and report delivery. Browser PDF generation consumes the `insights` array and can reflect these findings, but it remains unverified here. **This is an on-screen findings-and-tracker subsection certificate only. The certificate and read-only validation script are stored on a separate documentation branch so production `main` remains at the exact certified SHA. No master ledger update, whole-Insights claim, or Reports claim was made.** Recheck this certificate if finding logic/dependencies, selected source configuration, or provider evidence changes.
