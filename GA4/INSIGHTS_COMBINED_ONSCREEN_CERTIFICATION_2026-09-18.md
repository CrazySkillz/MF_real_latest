# GA4 Insights combined on-screen certification — 2026-09-18

## Decision and exact boundary

**HISTORICAL EXACT-RUNTIME EVIDENCE for the combined, on-screen GA4 Insights page** on deployed application revision `f74167597d8f9aad09a43466f3a94ce3b7c7cedd`, for owned Campaign2 (`eee3e654-b736-4e8e-86ec-1050e4d905c0`), its sole active OAuth property `542352127`, saved filters `yesop_retargeti` and `yesop_email_nurture`, USD, and `Europe/Amsterdam`. The later V1 financial-boundary correction invalidates this record as current production-readiness evidence. Current native financial values start at the saved GA4 initial-import date; imported Revenue and Spend include every mapped record.

This is a new bounded combined-page certificate, not an edit or renewal of the historical machine record for `4be16c54c550a45dbf3104313c820ea47b453604`. The four subsection certificates remain unchanged and supply their documented negative-state and lifecycle evidence; their older exact-runtime claims are not silently carried forward as current-runtime passes. The current revision was independently checked below.

## Source, value, and downstream inventory

| On-screen value | Source → API → calculation → display | Downstream boundary |
| --- | --- | --- |
| Executive Financials | Historical exact-runtime evidence: owner-scoped GA4 native Revenue (`GET /ga4-to-date`, Sep 8–17, USD) plus active imported Revenue (`GET /revenue-to-date`, through Sep 18), and active Spend (`GET /spend-to-date?platformContext=ga4`, through Sep 17) fed Revenue, Spend, Profit, ROAS, ROI, source labels, and the then-visible mixed-cutoff warning. The displayed values were `$67,674.40`, `$338.00`, `$67,336.40`, `200.22x`, and `19922.0%`; independent API arithmetic matched. This does not certify the current boundary, which uses the saved GA4 initial-import date for native values and every mapped imported record. | Existing Overview financial producers and source lifecycles are read-only dependencies, not recertified. Reports/PDF calculations are excluded. |
| Trends | The page's read-only selected-property 60-day GA4 daily GET and provider coverage GET feed the campaign-created-date-filtered Daily, 7d, 30d, and Monthly charts/tables. On this campaign, provider coverage reconciled all 14 stored dates and independently verified 26 absent dates as zero. Six Daily metrics, the 7d Sessions series, a correctly unavailable 30d chart, and the partial Monthly series matched independent calendar calculations. | The chart/table does not write stored facts. Its campaign-start display filter does not narrow Data Summary or findings history. |
| Data Summary | The same daily GET's imported-history `overviewTotals` and dates feed Sessions, Conversions, and conversion rate. A read-only database sum of selected campaign/property rows matched 531 Sessions, 91 Conversions, 776 Page Views, and `$18,098.70` native daily Revenue over Aug 9–Sep 17. The card showed 531, 91, and 17.1%, explicitly labeled as imported history; it includes pre-creation imported dates. | Disabled Top Channel, source/medium rows, and channel warnings are not part of this certificate. Summary traffic is not substituted for the financial GA4 to-date query. |
| Findings and tracker cards | The selected daily/coverage result, campaign-scoped financial inputs, and existing saved-target consumers feed one findings array. Its visible `positive:conversions:wow` finding matched the 16.2% seven-day increase, and the Total/High/Medium cards matched the generated array at 1/0/0 with no hidden finding. | Live configured KPI/Benchmark target findings were not exercised because this campaign has no such saved targets. Browser/server PDF and report consumers remain unverified here. |

The historical GA4 to-date endpoint also returned 316 Sessions for Sep 8–17, while date-dimension daily rows summed to 318 Sessions for that range. Those endpoints have different GA4 query shapes; this observation is **not** explained away as a proven GA4 rule, nor treated as a financial arithmetic input. No cross-query Sessions equality is claimed. The then-observed native financial Revenue for Sep 8–17 was `$9,997.50`, matching daily Revenue for that period; current native financial values use the saved initial-import window instead.

## Exact-revision validation and failure states

| Gate | Result |
| --- | --- |
| Deployment, owner, property, currency | PASS. Production `/api/health`, local HEAD, and `origin/main` reported `f74167597d8f9aad09a43466f3a94ce3b7c7cedd`. Read-only inventory joined Campaign2 to its owned client and sole active primary OAuth property, confirmed both saved filters, USD, and Amsterdam time. The authenticated owner page and selected-property APIs succeeded; unauthenticated daily GET returned 401. The shared campaign access guard returns 404 for a non-owner and is unchanged from its earlier live non-owner check; focused current auth/scope tests passed. No new production non-owner account was created or impersonated. |
| Scoped refresh and source accuracy | PASS. With explicit user approval, the existing owner-guarded `POST /api/campaigns/:id/ga4-daily-scheduler/run-now` ran only Campaign2 and its sole active property. It finished successfully at `2026-09-18T12:40:49.753Z`, refreshed daily facts and dependent KPI/Benchmark recompute, and suppressed the global alert sweep. The daily response reported `refreshIsStale:false`, `validationReadOnly:true`, and no provider refresh attempt for the later read-only check. The provider coverage GET then returned `verified:true`, 14 reconciled stored rows, and 26 independently verified zero dates. The earlier provider/storage mismatch resolved; saved Page Views changed from 773 to 776 while imported Sessions remained 531. This manual success does **not** prove a naturally fired daily timer on this revision or global scheduler health. |
| Complete live page and cross-section arithmetic | PASS. Authenticated GET-only browser checks on the exact revision reconciled each displayed financial value with page-consumed APIs, Data Summary with selected-property database sums, six Daily metric series and 7d/30d/Monthly states with the provider-verified daily/zero calendar, and visible findings with all three tracker counts. The page disclosed the different financial cutoffs. A read-only database transaction was rolled back; temporary Clerk sessions were revoked. |
| Unavailable, stale, and valid zero | PASS within the on-screen boundary. On the deployed bundle, browser-only intercepted daily failure withheld Data Summary numbers and Trends chart and produced the daily-history-unavailable finding. A stale last-good daily fixture showed Data Summary and Trends warnings, withheld the positive trend finding, and produced the stale-history finding. These fixtures made no production data changes. The live provider coverage verified 26 actual no-activity dates; current focused regressions cover missing versus valid zero, coverage mismatch, currency, financial unavailable/zero, source-label fallback, findings counts/deduplication, and target-list failures. A real future provider outage was not induced. |
| Open-page update | PASS for the daily-history polling consumer. On the exact deployed browser bundle, an accelerated five-minute interval with a browser-only daily-response fixture updated the already-open Data Summary from 91 to 92 Conversions without reopening the page or changing production data. The test did not claim a naturally fired server scheduler or live source change. A separate synthetic window-focus attempt did not trigger a request in the test harness and is not counted as evidence for focus refetch. |
| Engineering gates | PASS on this exact source revision: 19 focused Insights and adjacent files / 104 tests, `npm run check`, and `npm run build`. The product code did not change during this certification. The older `npm run check:ga4-insights-certification` gate is **not passing** because its `4be16c54` dependency hashes predate later approved changes; it governs the preserved historical machine record and is not presented as a pass for this new certificate. The separately documented report-copy regression is also not counted as passing. |

## Lifecycle, exclusions, and invalidation

Insights is a read-only consumer: it has no own add/edit/delete. The relevant source add/edit/delete paths retain their separate certificates and are not revalidated here. The manual refresh, subsequent provider/storage reconciliation, current UI/API value propagation, accelerated open-page daily poll, and browser-only failure states are the refresh lifecycle evidence for this exact page boundary. Other source-backed financial queries have their existing refresh intervals and separate source certificates; an exact-runtime natural server timer firing was not performed or claimed.

Excluded: other campaigns, clients, GA4 properties, saved filters, currencies, or source configurations; live configured KPI/Benchmark target findings; Google Ads (not configured); channel breakdown (disabled); Reports, browser/server PDFs, report snapshots, scheduled delivery, and email; global scheduler health; natural timer firing on this commit; future GA4 data changes and real provider outages. The earlier four subsection certificates and protected Overview, KPIs, Benchmarks, Ad Comparison, financial-source certificates, code, tests, contracts, and historical Insights machine record were not changed. Recheck this combined certificate when the deployed revision, selected source configuration, provider parity, or shared input behavior changes.
